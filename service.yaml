apiVersion: v1
kind: Service
metadata:
  name: devops-app
  namespace: devops-lab
  labels:
    app: devops-app
spec:
  type: NodePort
  selector:
    app: devops-app
  ports:
  - port: 80
    targetPort: 8080